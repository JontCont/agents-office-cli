## 1. HTTP Server Restructure

- [ ] 1.1 Restructure the HTTP server in `cmd/agent-office/main.go` `handleGUI()` to use an explicit `http.ServeMux` that routes `/ws` to the WebSocket upgrade handler, `/api/` prefix to REST handlers, and `/` to the static file server. Behavior: `/api/agents` and `/api/config` return JSON responses; `/ws` upgrades to WebSocket; `/` serves embedded GUI files — all on the same port without path conflicts. Verification: run `agent-office gui`, confirm `curl http://localhost:8080/api/agents` returns JSON and `curl http://localhost:8080/` returns HTML.

## 2. Provider-Aware Model Listing

- [ ] 2.1 Implement provider-aware model listing in `cmd/agent-office/main.go` `handleModels()`. Behavior: `agent-office models` reads `default_provider` from `.agent-office-token`; if `openrouter`, calls `GET https://openrouter.ai/api/v1/models` with the stored Bearer token and prints each `data[].id` one per line; for `openai`/`gemini`/`anthropic`, prints only the static curated list for that provider; if no provider configured, exits 1 with `"No provider configured. Run 'agent-office login' first."`. Verification: run `agent-office models` after `login --provider openrouter <token>` → output contains OpenRouter model IDs only, not gpt-4o/gemini/claude.
- [ ] 2.2 Add curated static model lists for `openai`, `gemini`, and `anthropic` in `pkg/workforce/provider.go` as a `ProviderStaticModels` map. Behavior: `GetStaticModels(provider)` returns a slice of model ID strings for that provider; empty slice if provider unknown. Verification: unit test in `pkg/workforce/provider_test.go` asserts correct model IDs for each static provider.
- [ ] 2.3 Add `ProviderCostPer1KTokens` map in `pkg/workforce/provider.go` with rates: `openai=0.005`, `gemini=0.0005`, `anthropic=0.008`, `openrouter=0.002`. Behavior: `GetCostPer1KTokens(provider)` returns the float64 rate for the given provider. Verification: unit test asserts correct rate for each provider; returns 0.0 for unknown providers.
- [ ] 2.4 Implement `GET /api/config` REST handler in `cmd/agent-office/main.go`. Behavior: returns `{"provider":"<active-provider>","cost_per_1k":<rate>}` as JSON by reading `.agent-office-token` and looking up the cost rate. Verification: `curl http://localhost:8080/api/config` while running as `openrouter` returns `{"provider":"openrouter","cost_per_1k":0.002}`.

## 3. Agent Management — CLI

- [ ] 3.1 Implement `agent-office agent create` interactive CLI command in `cmd/agent-office/main.go`. Behavior: prompts `Agent name:`, `Role:`, `Backstory:` using `bufio.NewReader`; reads `agent-office.yaml`, appends the new `AgentConfig`, writes file back; prints `Agent '<name>' created successfully.`; if yaml missing, prints error and exits 1. Verification: run `agent-office agent create`, fill in values, then `agent-office agent list` shows the new agent.
- [ ] 3.2 Add `SaveConfig(path string, cfg *Config) error` function to `pkg/config/config.go`. Behavior: marshals `*Config` to YAML using `gopkg.in/yaml.v3` and writes to the given path with `0644` permissions. Verification: unit test writes a config, reads it back, confirms agents list matches.

## 4. Agent Management — REST API and GUI

- [ ] 4.1 Implement `GET /api/agents` handler in `cmd/agent-office/main.go`. Behavior: reads `agent-office.yaml` and returns `{"agents":[{"name":"...","role":"...","backstory":"..."}]}` with HTTP 200; returns HTTP 500 with `{"error":"configuration not found"}` if yaml missing. Verification: `curl http://localhost:8080/api/agents` returns correct JSON with existing agents.
- [ ] 4.2 Implement `POST /api/agents` handler in `cmd/agent-office/main.go`. Behavior: parses JSON body `{"name":"...","role":"...","backstory":"..."}`, validates that `name` and `role` are non-empty (returns HTTP 400 `{"error":"name and role are required"}` otherwise), appends agent to yaml via `SaveConfig`, returns HTTP 201 with created agent as JSON. Verification: POST a valid agent → `agent-office agent list` shows it; POST with empty name → 400 response.
- [ ] 4.3 Add Agents tab to the GUI in `cmd/agent-office/gui/index.html` with a tab bar switching between "Run Thread" and "Agents" views. The Agents view SHALL contain a table with columns Name, Role, Backstory and an "Add Agent" button that reveals an inline form with inputs for name, role, and backstory and a Submit button. Verification: open `http://localhost:8080`, click Agents tab — table and Add Agent button are visible.
- [ ] 4.4 Implement Agents tab JavaScript logic in `cmd/agent-office/gui/src/main.js`. Behavior: on tab switch to Agents, fetch `GET /api/agents` and populate the table; on Add Agent Submit, POST to `/api/agents` and refresh the table; clear the form on success. Verification: add an agent via the GUI form → it appears in the table immediately without page reload.
- [ ] 4.5 Add CSS styles for the tab bar, agent table, and Add Agent form in `cmd/agent-office/gui/src/style.css`. Behavior: tab bar is visually distinct with active/inactive states; agent table rows have hover effects; Add Agent form is hidden until button clicked, then slides in. Verification: visual check in the browser — no layout overflow, consistent with existing dark theme.

## 5. Session Usage Tracking

- [ ] 5.1 Add `SessionLog` struct and `WriteSessionLog` function in `pkg/workforce/provider.go`. Behavior: `WriteSessionLog(log SessionLog)` creates `.agent-office-sessions/` if absent and writes `<YYYY-MM-DD>-<runID>.json` with fields `run_id`, `provider`, `model`, `started_at`, `ended_at`, `total_tokens`, `estimated_cost_usd`, `steps`. Verification: unit test calls `WriteSessionLog`, reads back the JSON, asserts all fields match.
- [ ] 5.2 Integrate session log writing into `startMockRun` in `cmd/agent-office/main.go`. Behavior: record `startedAt` at run start; on `StateCompleted` or `StateCancelled` transition, call `WriteSessionLog` with accumulated token total, step count, provider, model, and timestamps. Verification: run `agent-office gui`, let mock run complete, confirm `.agent-office-sessions/<date>-<runID>.json` exists and contains valid JSON with correct fields.
- [ ] 5.3 Implement `GET /api/session/latest` REST handler in `cmd/agent-office/main.go`. Behavior: returns `{"path":".agent-office-sessions/<filename>"}` for the most recently written session log, or `{"path":""}` if none exist. Verification: after a completed run, `curl http://localhost:8080/api/session/latest` returns the correct path.
- [ ] 5.4 Add `Est. Cost (USD)` metric box and `Session Log` label to the telemetry panel in `cmd/agent-office/gui/index.html`. Behavior: `Est. Cost (USD)` updates in real-time as tokens accumulate; `Session Log` shows the file path after run completion fetched from `GET /api/session/latest`. Verification: run `agent-office gui`, watch run complete — cost field updates per step, session log path appears after COMPLETED state.
- [ ] 5.5 Implement cost calculation and session log display logic in `cmd/agent-office/gui/src/main.js`. Behavior: on page load, fetch `GET /api/config` to get `cost_per_1k`; on each `agent.speak` event, compute `estCost = (totalTokens / 1000) * costPer1k` and update `#metric-cost`; on `state.change` to `COMPLETED` or `CANCELLED`, fetch `GET /api/session/latest` and display the path in `#session-log-path`. Verification: run `agent-office gui`, verify `#metric-cost` shows non-zero value matching the expected formula after mock steps complete.
